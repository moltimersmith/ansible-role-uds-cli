#SPDX-License-Identifier: MIT-0
---

# Manage UDS CLI (uds)

# Desired state of UDS CLI
# - present: ensure installed
# - absent:  uninstall
uds_cli_state: present

# Homebrew settings (macOS)
uds_cli_brew_update: false
uds_cli_brew_taps:
  - defenseunicorns/tap
uds_cli_brew_formulae:
  - uds
